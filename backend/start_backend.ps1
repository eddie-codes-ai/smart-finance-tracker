# All secrets/config now live in backend/.env (loaded automatically via
# python-dotenv in app.py) instead of being set here. See .env.example
# if you need to recreate it from scratch.

.\venv\Scripts\Activate
python app.py
